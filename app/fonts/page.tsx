import FontsPageClient from "@/components/FontsPageClient";
export const metadata = {
  title: "Browse Fonts — FontsVerse",
  description: "Browse and download hundreds of custom fonts. Filter by category, sort by popularity, and get embed code for any framework in seconds.",
};
export default function FontsPage() { return <FontsPageClient />; }
